<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://kit.fontawesome.com/8e8731510e.js" crossorigin="anonymous"></script>
  <link rel="stylesheet" href="/css/templatemo-grad-school.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    .video-container {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-left: 80px;
      margin-top: 40px;
    }

    .video {
      flex: 1;
      margin-top: 50px;
      margin-right: 60px;
      max-height: 300px;
    }

    .description {
      flex: 1;
      margin-top: 20px;
      background-color: rgba(255, 255, 255, 0.9);
      border-radius: 5px;
      padding: 10px;
      max-height: 260px;
      overflow-y: auto;
      margin-right: 80px;
      margin-top: 50px;
    }

    .description pre {
      margin: 10px;
      white-space: pre-wrap;
    }

    .fixed-header {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      background-color: #162238;
      color: white;
      z-index: 1000;
      height: 60px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding-left: 20px;
    }

    .fixed-header ul li {
      margin-left: 25px;
    }

    .back-button {
      background-color: #f5a425;
      color: #162239;
      border: none;
      border-radius: 2px;
      padding: 7px 10px;
      cursor: pointer;
      font-size: 16px;
      transition: background-color 0.3s;
      margin-right: 60px;
    }

    .back-button:hover {
      background-color: #162239;
      color: #f5a425;
    }

    .logo img {
      height: 40px;
      width: 120px;
    }

    pre {
      margin: 0;
    }

    body {
      background-image: url(/images/choosing-bg.jpg);
      padding-left: 30px;
      padding-right: 30px;
      color: white;
    }

    article {
      margin-top: 50px;
    }

    a {
      color: #f5a425;
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
    }

    b {
      font-size: 1.5em;
      font-weight: bold;
    }

    .t1 {
      padding-top: 20px;
    }

    .description {
      font-size: 1em;
      color: black;
    }

    @media (max-width: 900px) {
      .video-container {
        display: block;
        margin: 10px 0;
      }

      .video, .description {
        width: 100%;
        height: 400px;
      }

      .description {
        margin-top: 10px;
      }

      .video iframe {
        width: 100%;
        height: 100%;
      }

      .fixed-header {
        flex-direction: row;
        align-items: center;
      }

      .fixed-header .logo img {
        height: 30px;
        width: 100px;
      }

      .fixed-header ul {
        margin: 0;
        padding: 0;
        display: flex;
      }

      .fixed-header ul li h2 {
        font-size: 20px;
        padding-top: 10px;
      }

      .back-button {
        font-size: 14px;
        padding: 5px 8px;
        margin-top: 10px;
      }
    }
  </style>
</head>
<body>
  <header class="fixed-header">
    <ul>
      <li type="none">
        <h2 class="t1" style="color: #f5a425;"><%= data.title %></h2>
      </li>
    </ul>
    <div class="logo">
      <img src="/images/logo.png" alt="no img">
    </div>
    <button class="back-button" onclick="window.history.back();">Back</button>
  </header>

  <article class="article">
    <% if (data.link) { %>
      <% const videoLinks = data.link.split(','); %>
      <% const videoDescriptions = data.description.split('NEXT_DESCRIPTION'); %>
      <% videoLinks.forEach((link, index) => { %>
        <div class="video-container">
          <div class="video">
            <iframe width="460" height="260" 
              src="https://www.youtube.com/embed/<%= link.split('youtu.be/')[1].split('?')[0] %>" 
              title="YouTube video player" 
              frameborder="0" 
              allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
              allowfullscreen>
            </iframe>
          </div>
          <div class="description">
            <pre style="color: black;">
              <%- videoDescriptions[index] || 'No description provided' %>
            </pre>
          </div>
        </div>
      <% }); %>
    <% } else { %>
      <p>No video available.</p>
    <% } %>
  </article>

  <footer class="footer-container">
    <div class="container">
      <div class="row align-items-center">
        <div class="col-md-6 text-md-start text-center mb-3 mb-md-0">
          <div class="logo">
            <img src="/images/logo.png" alt="Logo">
          </div>
        </div>
        <div class="col-md-6 text-md-end text-center">
          <h2>Follow us on</h2>
          <a href="https://facebook.com" target="_blank" aria-label="Facebook"><i class="fab fa-facebook-f"></i></a>
          <a href="https://twitter.com" target="_blank" aria-label="Twitter"><i class="fab fa-twitter"></i></a>
          <a href="https://instagram.com" target="_blank" aria-label="Instagram"><i class="fab fa-instagram"></i></a>
          <a href="https://linkedin.com" target="_blank" aria-label="LinkedIn"><i class="fab fa-linkedin-in"></i></a>
          <a href="https://youtube.com" target="_blank" aria-label="YouTube"><i class="fab fa-youtube"></i></a>
        </div>
      </div>
    </div>
  </footer>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>