<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign In and Register</title>
  <style>
    body {
      background-image: url(/images/choosing-bg.jpg);
      background-size: cover; /* Cover the entire background */
      color: white;
      font-family: 'Poppins', sans-serif; /* Change font for better aesthetics */
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column; /* Stack elements vertically */
      height: 100vh; /* Full height of the viewport */
      margin: 0; /* Remove default margin */
      text-align: center; /* Center text */
    }

    h2 {
      color: #f5a425; /* Title color */
      margin-bottom: 20px; /* Space below title */
      font-size: 2.5rem; /* Adjust font size for the title */
    }

    h3 {
      margin-top: 40px; /* Space above register title */
      margin-bottom: 10px; /* Space below register title */
      font-size: 1.5rem; /* Adjust font size for register */
    }

    form {
      /* Semi-transparent background for forms */
      padding: 20px; /* Padding inside forms */
      border-radius: 8px; /* Rounded corners */
      margin-bottom: 20px; /* Space below each form */
      width: 100%; /* Full width */
      max-width: 400px;
    }

    label {
      display: block; /* Block display for labels */
      margin: 10px 0 5px; /* Margin for labels */
      font-weight: bold; /* Bold text for labels */
      font-size: 1.1rem; /* Slightly larger font size for labels */
    }

    input[type="text"],
    input[type="password"] {
      width: 100%; /* Full width */
      padding: 12px; /* Padding for inputs */
      margin-bottom: 15px; /* Space below inputs */
      border-radius: 5px; /* Rounded corners */
      border: 2px solid #555; /* Border color */
      background: rgba(255, 255, 255, 0.2); /* Light background for inputs */
      color: white; /* Text color */
      font-size: 1rem; /* Font size for inputs */
      transition: border-color 0.3s; /* Smooth transition for border color */
    }

    input[type="text"]:focus,
    input[type="password"]:focus {
      border-color: #f5a425; /* Change border color on focus */
      outline: none; /* Remove default outline */
      box-shadow: 0 0 5px rgba(245, 164, 37, 0.7); /* Shadow effect */
    }

    input[type="submit"] {
      background-color: #f5a425; /* Button background color */
      border: none; /* Remove border */
      color: white; /* Button text color */
      padding: 12px; /* Padding for button */
      cursor: pointer; /* Pointer cursor on hover */
      border-radius: 5px; /* Rounded corners for button */
      font-size: 1rem; /* Font size for button */
      transition: background-color 0.3s; /* Smooth transition */
      width: 100px;
    }

    input[type="submit"]:hover {
      background-color: #e68a1f; /* Darker shade on hover */
    }
  </style>
</head>
<body>
  
  <h2>Sign In</h2>
  
  <form action="/admin" method="POST">
    <label for="username"><b>Username:</b></label>
    <input type="text" placeholder="Enter Username" name="username" required>
    
    <label for="password"><b>Password:</b></label>
    <input type="password" placeholder="Enter Password" name="password" required>
  
    <input type="submit" value="Login">
  </form>
  
  <!-- <h3>Register</h3>
  <form action="/register" method="POST">
    <label for="register-username"><b>Username:</b></label>
    <input type="text" placeholder="Enter Username" name="username" required>
    
    <label for="register-password"><b>Password:</b></label>
    <input type="password" placeholder="Enter Password" name="password" required>
  
    <input type="submit" value="Register">
  </form> -->
</body>
</html>
