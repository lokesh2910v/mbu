<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>View / Edit Courses</title>
  <style>
    body {
      padding: 40px;
      padding-top: 0;
      background-image: url(/images/choosing-bg.jpg);
      color: white;
      font-size: 20px; 
      font-family: 'Poppins', sans-serif; 
    }
    a {
      color: white;
      text-decoration: none; 
      margin-bottom: 20px;
      display: inline-block; 
    }

    .admin-title {
      margin-bottom: 20px;
    }

    input[type="text"],
    input[type="email"],
    input[type="password"],
    input[type="search"],
    select,
    textarea {
      display: block;
      box-sizing: border-box;
      width: 100%;
      padding: 0.7rem 0.75rem;
      margin-bottom: 1rem;
      font-family: inherit;
      font-size: inherit; 
    }

    .btn {
      background-color: #007bff;
      border: none;
      color: white; 
      padding: 12px 20px;
      text-decoration: none;
      margin: 4px 2px; 
      cursor: pointer;
      display: inline-block;
      width: 120px; 
      transition: background-color 0.3s; 
      font-size: 16px; 
      text-align: center; 
    }

    .btn:hover {
      background-color: #0056b3; 
    }

    .btn-delete {
      padding: 12px 20px;
      background-color: #dc3545; 
    }

    .btn-delete:hover {
      background-color: #c82333; 
    }
  </style>
</head>
<body>
  <a href="/dashboard" class="btn">&larr; Back</a> 
  <div class="admin-title">
    <h2 style="color: #f5a425;">View / Edit Courses</h2>
  
    <form action="/delete-post/<%= data._id %>?_method=DELETE" method="POST">
      <input type="submit" value="Delete" class="btn-delete btn">
    </form>
  
  </div>
  
  <form action="/edit-post/<%= data._id %>?_method=PUT" method="POST">
    <label for="title" style="color: #f5a425;"><b>Title</b></label><br><br>
    <input type="text" placeholder="Post Title" name="title" value="<%= data.title %>" required>
  
    <label for="link" style="color: #f5a425;"><b>Link Body</b></label><br><br>
    <textarea name="link" cols="50" rows="8" required><%= data.link %></textarea> <!-- Link body textarea -->
    
    <label for="description" style="color: #f5a425;"><b>Description Body</b></label><br><br>
    <textarea name="description" cols="50" rows="8" required><%= data.description %></textarea> <!-- Description body textarea -->
  
    <input type="submit" value="Update" class="btn">
  </form>
</body>

</html>
